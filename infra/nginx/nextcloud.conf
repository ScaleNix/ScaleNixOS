# Nextcloud extra headers for iframe embedding in ScalenixOS
# Add to Nextcloud's nginx config

# Allow embedding in ScalenixOS shell
add_header Content-Security-Policy "frame-ancestors https://os.scalenix.fr" always;

# Remove X-Frame-Options (replaced by CSP frame-ancestors)
proxy_hide_header X-Frame-Options;
